# 2026-07-18 ngmembers Development Log

## Work Summary

회원앱을 Supabase 실서비스로 전환하기 전에 Phase 0 기준선을 확정했다. 기능이나 UI를 변경하지 않고 현재 구조·데이터 흐름·빌드·개인정보 노출·최종 CSV·웹폰트를 점검했으며, 재현 가능한 결과를 `docs/reports/phase-0-baseline.md`에 기록했다.

점검 과정에서 명령은 성공하지만 최신 `src`가 실제 프로덕션 번들에 포함되지 않는 빌드 entry 문제와, 실제 회원 데이터가 public GitHub 저장소 및 GitHub Pages 번들에 포함된 보안 문제를 확인했다.

이후 기존 원격 저장소를 삭제하고 같은 이름의 새 저장소를 만든 상황을 기준으로, 구현 전에 Phase 1의 안전 기준·작업 순서·완료 조건을 정리했다. 초보 사용자가 전체 개발 순서를 빠르게 파악할 수 있도록 루트에 간단한 `ROADMAP.md`도 추가했다.

J님 승인 후 Phase 1 로컬 구현을 진행했다. 실제 회원 seed·CSV와 과거 번들을 제거하고 정상 Vite entry를 복구했으며, 개인정보 없는 fixture 위에 Supabase client, Google Auth, 고정 workspace Admin 권한 gate, members schema와 RLS를 추가했다. 빌드와 과거 seed marker 기반 안전 검사는 완료했다. 실제 Supabase project 적용은 시작됐지만 공용 schema 차이로 `002` 수정·재실행이 필요하며, 두 Auth UUID 등록·라이브 RLS 검증·안전한 새 Git 이력 확정도 남아 있다.

## Work Completed

- 작업 계획서를 끝까지 읽고 Phase 0 외의 Supabase 구현은 수행하지 않았다.
- 프로젝트의 React 컴포넌트, storage, CSV 변환, 관계 계산, 스타일과 빌드 구성을 분석했다.
- `npm ci`를 실행해 69개 package를 lockfile 기준으로 설치했고 audit 취약점 0건을 확인했다.
- `npm run build`를 실행해 TypeScript 검사와 Vite 빌드 성공을 확인했다.
- 빌드가 4개 모듈만 변환하고 과거 root JS/CSS를 다시 입력으로 사용하는 문제를 문구 대조로 확인했다.
- 최종 CSV 1,378행을 엄격 검증했다.
  - 오류 6행: 생년월일 형식 5행, 자기 sponsor 1행
  - 경고 84건/82행: 빈 회원번호·이름·국가, sponsor 미해결·복수 후보, CPF·전화번호 형식, 공백 CPF
  - 비어 있지 않은 회원번호 중복, boolean, side, 상태값 오류는 없었다.
- 최종 CSV와 현재 seed/sample이 각각 1,378행과 1,386행으로 다르고 회원 구성과 주요 플래그도 다른 것을 확인했다.
- seed JSON, sample CSV, root JS, dist JS, 일부 devlog와 계획서의 개인정보 포함 범주를 값 노출 없이 분류했다.
- 원격 저장소가 public이고 GitHub Pages가 실제 seed 개인정보가 포함된 번들을 공개하는 것을 확인했다.
- 현재 Pretendard가 jsDelivr 외부 CSS와 WOFF2로 연결되고 프로젝트·dist에는 로컬 폰트 파일이 없음을 확인했다.
- 현재 UI 문구와 승인 계획의 차이, GitHub Issue #1·#2와 계획서의 충돌 항목을 정리했다.
- `docs/plans/phase-1-supabase-foundation-plan.md`에 개인정보 격리, 정상 Vite entry, Supabase schema, allowlist, RLS, Google Auth와 검증 순서를 작성했다.
- `ROADMAP.md`에 Phase 0부터 Phase 5까지의 목적과 현재 위치를 간단히 정리했다.
- 실제 `seedMembers.json`, sample CSV, 개인정보가 포함된 과거 root bundle을 Git 작업 트리에서 제거했다.
- 기존 개발 로그와 장기 계획서의 실제 회원 예시를 가상 예시로 바꾸고, private data·report·환경변수 패턴을 `.gitignore`에 추가했다.
- `index.html`을 `/src/main.tsx` Vite entry로 복구하고 개인정보 없는 가상 회원 3명 fixture로 기존 회원 UI를 유지했다.
- 과거 실제 데이터용 `localStorage` key를 앱 시작 시 제거하고 Phase 1 demo 저장소와 분리했다.
- `@supabase/supabase-js`와 publishable key 전용 client를 추가하고 secret/service role key 감지 시 앱 진입을 차단했다.
- Google OAuth 로그인, session 복원, 로그아웃, 권한 확인, 미허용 사용자와 오류 상태 화면을 구현했다.
- `auth.uid()`와 고정 ngmembers workspace의 Admin 멤버십 행을 확인하는 RPC와 members SELECT·INSERT·UPDATE RLS를 작성했다.
- 일반 브라우저의 workspace membership 쓰기 권한과 members DELETE 권한을 회수하고 DELETE policy를 만들지 않았다.
- `npm ci`, `npm run build`, `npm run verify:phase1`을 실행해 취약점 0건, 90-module 정상 build, 5,410개 과거 seed marker 검사 통과를 확인했다.
- 로컬 브라우저에서 환경변수 누락 시 설정 안내만 표시되고 회원 shell·목록은 렌더링되지 않으며 콘솔 오류와 가로 overflow가 없음을 확인했다.
- `docs/reports/phase-1-implementation.md`에 변경 파일, SQL 적용 순서, 검사 결과, 수동 Dashboard 설정과 남은 완료 조건을 기록했다.
- 실제 Supabase에서 `002_workspace_access.sql` 실행 시 기존 `workspace_members`에 `is_active`가 없어 PostgreSQL 42703 오류가 발생한 과정을 확인했다. 전달받은 컬럼 목록에 맞춰 `is_active`와 `updated_at` 의존성을 제거하고 SQL·등록 쿼리·계획 문서를 동기화했다.
- 실제 Supabase REST에 publishable key만 사용해 무작위 UUID DELETE를 요청했고 HTTP 401로 거부되는 것을 확인했다. Admin 두 계정은 같은 `authenticated` role을 사용하며 해당 role과 `public`·`anon`의 DELETE grant가 모두 회수되고 DELETE policy도 없어 계정별 우회가 불가능하다.
- GitHub Issue #1을 생성하고 검증된 현재 tree만으로 부모 없는 root commit `7b4d810`을 만들었다. public 원격 `main`을 force-with-lease로 이 안전한 root에 맞춘 뒤 로컬 `main`, reflog와 연결되지 않은 과거 PII object도 정리했다.

## Decisions

- 기존 원격과 일치하는 `3c67826`을 변경 전 기준 커밋으로 사용하고 의미 없는 빈 커밋은 만들지 않았다.
- Phase 0에서는 발견된 빌드·PII·CSV 문제를 수정하지 않고 분석 결과만 문서화했다. 이는 현재 UI를 보존하고 다음 단계의 승인 범위를 분명히 하기 위해서다.
- 최종 CSV 원본과 실제 개인정보 값은 저장소 보고서에 복사하지 않았다. 문제 식별에는 CSV 행 번호와 필드 유형만 사용했다.
- sponsor 검증은 계획대로 이름·닉네임·회원번호의 정확 일치를 사용하고 공백과 대소문자만 정규화했다.
- public 저장소와 Pages의 PII 노출 차단을 Phase 1보다 먼저 결정해야 하는 최우선 사항으로 분류했다.
- 현재 index entry 문제를 해결하기 전에는 `npm run build` 성공을 최신 앱 배포 성공으로 간주하지 않기로 했다.
- entry 복구는 단독으로 먼저 배포하지 않고 실제 seed·과거 bundle 제거와 함께 수행하기로 했다. 정상 entry가 현재 seed를 다시 bundle하는 위험을 피하면서도 Phase 1 초반에 최신 build 기준선을 확보하기 위한 결정이다.
- 새 public 저장소에는 PII 검사에 통과한 안전한 첫 commit만 올리고, Phase 1 계획 문서 작성 단계에서는 구현이나 push를 수행하지 않기로 했다.
- J와 엄마는 앱 안에서 허용되는 작업이 같으므로 두 계정을 모두 `admin`으로 사용하기로 했다. 별도 `editor` 역할은 실질적인 권한 차이 없이 코드와 RLS 분기만 늘리므로 사용하지 않는다.
- 두 Admin 계정 모두 조회·추가·수정·숨기기만 할 수 있으며 DELETE 정책과 삭제 기능은 만들지 않기로 했다. 숨긴 항목의 복원·영구 삭제는 앱 밖에서 J가 Supabase Dashboard를 통해 처리한다.
- 기존 엄마용 앱 Supabase project를 재사용하고 ngmembers workspace ID는 `00000000-0000-0000-0000-000000000003`으로 정했다. 같은 project에 연결된 Google OAuth Client도 재사용하는 방향으로 확정했다.
- 별도 `app_users` 허용 목록을 중복 생성하지 않고 기존 `workspaces`와 `workspace_members`를 재사용하기로 했다. 두 계정은 이메일 문자열 대신 기존 Supabase Auth 사용자 UUID로 ngmembers 워크스페이스에 연결한다.
- 공유 project 호환성을 위해 `workspace_members.role` 전체를 Admin만 허용하는 check constraint는 만들지 않고, ngmembers 권한 helper에서만 `admin`을 요구하기로 했다.
- 실제 공용 `workspace_members`에는 활성 상태 컬럼이 없으므로 별도 컬럼을 자동 추가하지 않기로 했다. 기존 접근 모델을 보존해 멤버십 행의 존재를 활성 상태로 보고, 접근 회수는 해당 행 삭제로 처리한다.
- Phase 1에서는 실제 DB CRUD 전환을 앞당기지 않고 기존 UI를 가상 fixture와 Phase 1 전용 localStorage로 유지했다. 데이터 계층 전체 교체는 계획대로 Phase 3에 수행한다.
- 이전 Git seed를 메모리에서만 읽어 current source/dist와 대조하는 안전 검사기를 추가했다. 실제 marker 값이나 별도 개인정보 파일을 새 저장소에 기록하지 않으면서 잔존 여부를 재현 가능하게 확인하기 위한 결정이다.
- SQL은 기존 공용 schema를 자동 추측해 변경하지 않고 preflight에서 불일치를 중단하도록 했다. 기존 엄마용 앱의 테이블 구조를 모르는 상태에서 자동 migration으로 공유 기능을 깨뜨리지 않기 위해서다.
- DELETE 버튼 부재와 DB 권한 차단을 별도로 유지하기로 했다. UI는 실수를 막고, table grant와 RLS는 직접 REST 호출까지 차단하는 서로 다른 보안 계층이다.
- 새 공개 Git 이력은 기존 commit을 수정하는 방식이 아니라 현재 안전 tree를 부모 없는 root commit으로 만드는 방식을 선택했다. 원격에 PII 과거 object가 전달될 경로를 구조적으로 없애기 위해서다.
- 과거 object 정리 후에도 PII 대조 검사를 이어갈 수 있도록 ignore된 `scripts/pii-markers.local.txt`를 선택적으로 읽게 했다. 실제 marker를 Git에 복원하지 않으면서 Phase 2 이후 로컬 검증 자료를 사용할 수 있게 하기 위한 결정이다.

## Current Status

- Phase 0 분석과 보고서 작성은 완료됐다.
- `npm ci`와 `npm run build`는 성공했다.
- 최종 CSV는 6개 오류 행 때문에 아직 import 불가다.
- `member.nangok.app`은 현재 DNS에서 해석되지 않는다.
- Phase 1 실행 계획과 전체 로드맵 문서가 준비됐다.
- Phase 1 계획과 로드맵의 사용자 역할을 J·엄마 공통 Admin 구조로 정리했다.
- Phase 1 코드·SQL·문서의 로컬 구현과 빌드·정적 안전 검사는 완료됐다.
- `npm ci`는 Vite 개발 서버가 사용 중인 `esbuild.exe` 때문에 첫 실행에서 Windows `EPERM`이 발생했으나, 이번 작업에서 시작한 서버만 종료한 뒤 재실행해 성공했다.
- 실제 Supabase project에서 SQL 적용을 시작했지만 전체 적용 완료 여부는 아직 확인하지 않았으며, Auth UUID나 OAuth secret은 저장소·채팅에 입력하지 않았다.
- `002_workspace_access.sql`의 최초 schema 불일치는 수정됐고 J님이 실제 동작을 확인했다.
- 익명 DELETE는 실제 API에서 HTTP 401로 차단됐고 authenticated DELETE grant/policy도 없다.
- current source와 새 `dist`는 PII marker 검사를 통과했으며 로컬·원격 Git history도 안전한 root commit에서 시작한다.
- Phase 1은 완료됐다.

## Next Steps

- 최종 CSV 오류 6행과 경고 항목을 수정하거나 승인한 뒤 재검증한다.
- 계획서와 GitHub Issue #1·#2 사이의 문구·검색·닉네임·국가 표시 요구를 확정한다.
- Phase 2에서 최종 CSV 오류 6행과 경고 항목의 처리 결정을 확정한다.
- 개인정보를 공개 이력에 넣지 않는 검증·import script를 준비한다.
- production import 전 backup, dry run, 관계 연결 report 절차를 확정한다.
